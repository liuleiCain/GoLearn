# break

#### 介绍
for-range的迭代用法非常方便，但是有一些需要注意的点，这里记录一下

#### 整型的for-range
数字整型的for-range在go1.22之后支持

#### for-range的迭代变量
1.  迭代变量在每次循环中会重用，不会重新声明
2.  迭代变量的值来源于拷贝，而不是原始数据，如果需要修改迭代变量的值，需要使用指针
3.  go1.22版本之后，在for-range中使用i,v临时变量，会作为参数传递，不需要额外设置参数
